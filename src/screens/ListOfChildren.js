import ListOfChildrenPage from "@components/pages/ListOfChildren/ListOfChildren";

const ListOfChildren = ({ navigation }) => {
  return <ListOfChildrenPage navigation={navigation} />;
};

export default ListOfChildren;
