import ChangePasswordPage from "@components/pages/ChangePassword/ChangePassword";

const ChangePassword = ({ navigation }) => {
  return <ChangePasswordPage navigation={navigation} />;
};

export default ChangePassword;
