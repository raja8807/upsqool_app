import PhotoViewerScreenPage from "@components/pages/PhotoViewerScreen/PhotoViewerScreen";

const PhotoViewerScreen = ({ navigation }) => {
  return <PhotoViewerScreenPage navigation={navigation} />;
};

export default PhotoViewerScreen;
