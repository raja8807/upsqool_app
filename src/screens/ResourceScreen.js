import ResourceScreenPage from "@components/pages/ResourceScreen/ResourceScreen";

const ResourceScreen = ({ navigation }) => {
  return <ResourceScreenPage navigation={navigation} />;
};

export default ResourceScreen;
