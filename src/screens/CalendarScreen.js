import CalendarScreenPage from "@components/pages/CalendarScreen/CalendarScreen";

const CalendarScreen = ({ navigation }) => {
  return <CalendarScreenPage navigation={navigation} />;
};

export default CalendarScreen;
